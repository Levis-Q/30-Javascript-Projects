/*  
// function declaration

function checkIfNameExists(name) {

// Array of Nmaes Declaration
    let names = ['Mgijima', 'Kabelo', 'Shangwan', 'Reiko', 'John', 'Mnyokza']    

// check for matching names
    if(names.includes(name)) {
        return true;
    }else {
        return false;
    }
}
 
checkIfNameExists("Reiko");  // ANS1 DOESN'T RETURN FEEDBACK



function checkIfNameExists() {
    let names = ['Mgijima', 'Kabelo', 'Shangwan', 'Reiko', 'John', 'Mnyokza']

// loop and check for matching names
    for (let i = 0; i < names.length; i++) {
    if(names[i] === name) {
        return true;
    }
    return false;
    }
}

checkIfNameExists('Mnyokza');  // ANS2 DOESN'T RETURN FEEDBACK either
  */

